---
title: Source-Guided Flow Matching
title_zh: 源引导流匹配
authors: "Zifan Wang, Alice Harting, Matthieu Barreau, Michael M. Zavlanos, Karl Henrik Johansson"
date: 2026-01-26
pdf: "https://openreview.net/pdf?id=p56ZAQUCUr"
tags: ["query:dgs-fm"]
score: 8.0
evidence: 源引导流匹配用于生成建模
tldr: 该论文提出源引导流匹配（SGFM）框架，与传统引导方法修改向量场不同，SGFM直接修改源分布而保持预训练向量场不变，将引导问题转化为从源分布采样的子问题。理论证明可精确恢复目标分布，并提供Wasserstein误差界。该方法允许用户灵活选择采样策略，为流匹配生成模型提供了新的可控生成途径，适用于连续概率路径的引导生成。
source: ICLR-2026-Accepted
selection_source: conference_retrieval
motivation: 传统引导需修改概率流向量场，限制灵活性；源引导通过修改源分布保持向量场不变。
method: 直接修改源分布，用预训练向量场映射，实现精确目标分布恢复。
result: 理论上精确恢复目标分布，并提供误差界，支持灵活采样方法。
conclusion: 源引导为流匹配提供了一种灵活且理论保真的生成控制方式。
---

## Abstract
Guidance of generative models is typically achieved by modifying the probability flow vector field through the addition of a guidance field. In this paper, we instead propose the Source-Guided Flow Matching (SGFM) framework, which modifies the source distribution directly while keeping the pre-trained vector field intact. This reduces the guidance problem to a well-defined problem of sampling from the source distribution. We theoretically show that SGFM recovers the desired target distribution exactly. Furthermore, we provide bounds on the Wasserstein error for the generated distribution when using an approximate sampler of the source distribution and an approximate vector field. The key benefit of our approach is that it allows the user to flexibly choose the sampling method depending on their specific problem. To illustrate this, we systematically compare different sampling methods and discuss conditions for asymptotically exact guidance. Moreover, our framework integrates well with optimal flow matching models since the straight transport map generated by the vector field is preserved. Experimental results on synthetic 2D benchmarks, physics-informed generative tasks, and imaging inverse problems demonstrate the effectiveness and flexibility of the proposed framework.

---

## 论文详细总结（自动生成）

# 源引导流匹配（SGFM）论文详细总结


## 一、论文的核心问题与整体含义

**研究背景：** Flow Matching（流匹配）是一种生成建模框架，通过学习一个向量场，在固定时间内将概率流从源分布 $q_0$ 驱动至目标分布 $q_1$。在生成模型引导任务中，传统做法是通过向概率流向量场添加引导场来修改其方向。最优流匹配模型具有直传输映射（straight transport map）的优良特性，可大幅减少推理步数、提升计算效率。

**核心问题：** 传统引导方法修改向量场会破坏最优流匹配的直传输映射特性；同时，现有精确引导方法（如将引导建模为随机最优控制问题）需要为每个新的条件场景重新求解优化问题，计算成本高昂。

**研究动机：** 论文提出能否通过**只修改源分布、保持预训练向量场不变**来实现精确引导，从而避免上述问题。这一思路的核心洞察是：引导问题可以被转化为一个**从修改后的源分布中采样**的明确定义的问题。


## 二、方法论

### 2.1 核心思想

SGFM 框架的核心思想是：对于预训练的流匹配模型（向量场 $v_t$ 将 $q_0$ 传输到 $q_1$），若希望得到新的目标分布 $q_1’(x_1) \propto q_1(x_1) e^{-J(x_1)}$（其中 $J$ 为损失函数），则只需将源分布修改为：

$$q_0’(x_0) = \frac{1}{Z_0} q_0(x_0) e^{-J \circ T(x_0)}$$

其中 $T = \phi_1$ 是从 $q_0$ 到 $q_1$ 的精确传输映射。在此修改下，**相同的预训练向量场** $v_t$ 即可将 $q_0’$ 精确传输到 $q_1’$。

### 2.2 关键技术细节

**（1）理论精确性（Theorem 1）：** 在精确向量场条件下，SGFM 可精确恢复目标分布。证明基于 pushforward 测度的性质：$(\phi_1)_{\#} q_0’ = q_1’$。

**（2）误差界（Theorem 2）：** 在实际应用中，向量场是学习得到的近似值（误差 $\epsilon$），源分布采样也可能不精确（分布误差 $W_2(q_0’, \tilde{q}_0)$）。论文给出了生成分布与理想目标分布之间 2-Wasserstein 距离的上界：

$$W_2(q_1’, [\phi_1^{\theta}]_{\#} \tilde{q}_0) \leq e^{L_v} W_2(q_0’, \tilde{q}_0) + \epsilon e^{L_v}$$

该界表明：源分布采样误差和向量场近似误差都会被流映射的 Lipschitz 常数 $e^{L_v}$ 放大。

**（3）与最优流匹配的集成：** 当使用最优向量场时，轨迹为直线，大幅减少了 ODE 积分所需的步数。SGFM 保持向量场不变，因此完整继承了最优流匹配的直传输映射特性。

**（4）采样方法：** SGFM 允许用户根据具体问题灵活选择采样方法，包括：
- **重要性采样（IS）：** 低维空间中快速、无需梯度的采样方法
- **哈密顿蒙特卡洛（HMC）：** 高维空间中的 MCMC 方法
- **基于优化的采样：** 通过优化负对数似然寻找源分布的模


## 三、实验设计

### 3.1 实验场景

论文在以下三类任务上进行了实验验证：

1. **合成 2D 基准数据集（Synthetic 2D benchmarks）**
2. **物理信息生成任务（Physics-informed generative tasks）**
3. **成像逆问题（Imaging inverse problems）**

### 3.2 对比方法

论文系统比较了多种采样方法，并讨论了渐进精确引导的条件。对比的方法包括：
- 传统向量场引导方法（如修改向量场的现有工作）
- D-Flow 等启发式方法——论文指出 SGFM 与优化-based 采样结合时，可自然地恢复 D-Flow 的启发式公式，并为其提供理论 justification

### 3.3 Benchmark

论文将 SGFM 与现有引导方法进行对比，重点关注：
- 生成样本对目标分布的逼近精度
- 推理效率（ODE 积分步数）
- 轨迹的直线性保持


## 四、资源与算力

**论文中未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。仅从论文描述可知，SGFM 作为一种**无需训练（training-free）** 的引导框架，其核心优势之一是**避免为每个新条件场景重新训练或求解 SOC 问题**，因此在计算成本上相比现有方法具有显著优势。


## 五、实验数量与充分性

论文在**三个不同维度**的实验场景上进行了验证：
1. 合成 2D 基准（验证基础理论正确性）
2. 物理信息生成任务（验证在科学计算场景的适用性）
3. 成像逆问题（验证在图像领域的实用性）

此外，论文在方法论层面**系统比较了多种采样方法**（重要性采样、HMC、优化-based 采样），并讨论了各自的条件与适用场景。

**评估：** 实验覆盖了从低维合成数据到高维图像任务的多层次场景，具有较好的代表性。然而，由于论文摘要和公开信息有限，无法确认是否包含消融实验（如不同采样方法间的定量对比、不同损失函数 $J$ 的影响等）的详细结果。总体而言，实验设计在**多样性**上是充分的，但在**深度细节**上需参考论文全文。


## 六、主要结论与发现

1. **理论贡献：** SGFM 在理论上证明仅修改源分布即可精确恢复目标分布，提供了严谨的数学保证。

2. **误差可控：** 给出了源分布采样误差和向量场近似误差对生成质量的量化影响上界，为实际应用提供了理论指导。

3. **灵活性优势：** SGFM 允许用户根据具体问题灵活选择采样方法，适用于不同的计算资源和精度需求。

4. **最优流匹配兼容性：** SGFM 保持预训练向量场不变，因此完美保留了最优流匹配的直传输映射特性，推理效率更高。

5. **与现有方法的关系：** SGFM 为 D-Flow 等启发式方法提供了理论解释和自然扩展。

6. **实验验证：** 在合成数据、物理信息生成和成像逆问题三类任务上验证了框架的有效性和灵活性。


## 七、优点

1. **方法论创新性强：** 将引导问题从“修改向量场”范式转变为“修改源分布+采样”范式，思路简洁而深刻。

2. **理论完备：** 同时提供了精确性证明（Theorem 1）和误差界分析（Theorem 2），理论链条完整。

3. **计算高效：** 无需为每个新条件重新训练或求解 SOC，且保持最优流匹配的直线轨迹，推理步数少。

4. **灵活性强：** 用户可根据问题自由选择采样方法（IS、HMC、优化等），权衡精度与效率。

5. **兼容性好：** 与最优流匹配模型天然集成，不破坏其优良特性。

6. **理论联系实际：** 为 D-Flow 等已有启发式方法提供了理论支撑，具有解释力。


## 八、不足与局限

1. **依赖传输映射 $T$ 的获取：** 修改源分布需要计算 $J \circ T(x_0)$，其中 $T = \phi_1$ 是预训练模型的传输映射。在非最优流匹配模型中，$T$ 的获取仍需通过 ODE 积分，可能引入额外计算。

2. **高维采样的挑战：** 尽管 SGFM 将引导问题简化为采样问题，但高维空间中的精确采样本身仍是一个困难问题（如重要性采样的维数灾难）。

3. **凸性假设：** 在使用 HMC 时，理论上要求 $J \circ T^*$ 具有强凸性和 Lipschitz 连续的梯度，这一条件在实际中可能难以全局满足。

4. **优化-based 采样的模式坍塌风险：** 使用负对数似然优化时，可能将样本推向源分布的唯一模，导致模式坍塌。论文虽提出了基于 $\chi^2$ 分布范数正则化的改进方案，但这增加了方法复杂度。

5. **实验细节公开不足：** 从公开信息无法获取详细的实验设置、超参数、定量结果等，限制了可复现性评估。

6. **应用范围验证有限：** 虽然涵盖了物理信息和成像逆问题，但对于大规模文本到图像生成等更复杂的实际场景，验证尚不充分。


（完）
