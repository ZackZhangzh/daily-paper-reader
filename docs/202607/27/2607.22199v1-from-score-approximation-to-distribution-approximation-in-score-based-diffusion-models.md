---
title: From Score Approximation to Distribution Approximation in Score-Based Diffusion Models
title_zh: 基于分数的扩散模型中从分数逼近到分布逼近
authors: Lan V. Truong
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22199v1"
tags: ["query:fm"]
score: 9.0
evidence: 基于得分的扩散模型直接理论分析
tldr: 基于分数的扩散模型在生成任务中表现出色，但其近似理论基础尚不完整：神经网络对得分函数的逼近能否保证反向过程生成分布接近目标数据，缺乏定量刻画。本文建立两者间的严格联系，证明若得分近似足够好，则生成分布与目标分布在KL散度上接近，差异由前向过程终端分布与先验的失配决定。所得上界显式依赖得分误差、噪声计划和先验失配，为经典神经网络逼近理论在扩散模型中的应用提供了简洁保证。
source: arxiv
selection_source: fresh_fetch
motivation: 得分函数逼近的经典结果未能直接导出生成分布的质量保证，两者间的定量关系缺失。
method: 结合Hornik通用逼近定理、Girsanov路径空间定理和相对熵数据处理不等式，推导显式上界。
result: 生成分布与目标分布的KL散度受限于得分近似误差、噪声计划及终端先验失配之和。
conclusion: 证明了得分函数逼近精度可定量转化为生成分布逼近精度，为扩散模型提供理论支撑。
---

## 摘要
基于分数的扩散模型在生成建模中取得了显著的经验成功，但其近似理论基础尚不完整。特别地，尽管经典万能逼近定理保证神经网络可以逼近分数函数，但这种逼近保证能否转化为对逆向扩散过程生成的概率分布的逼近仍不清楚。本文建立了这两个概念之间严格的定量联系。具体而言，我们证明，如果神经网络足够精确地逼近真实分数函数，则相应的逆向扩散模型生成的概率分布与目标数据分布在Kullback-Leibler（KL）散度意义下接近，至多相差正向扩散过程的终端分布与用于初始化逆向过程的先验之间的不可约失配。更精确地说，我们推导了分布逼近误差关于分数逼近误差、扩散噪声调度和终端先验失配的显式上界。我们的分析结合了Hornik的万能逼近定理、路径空间上的Girsanov定理以及相对熵的数据处理不等式。作为近期在有限样本统计设定和数据分布结构假设下研究分数逼近工作的补充，本文基于经典神经网络逼近理论发展了近似理论分析。所得定理提供了简单显式的保证，将神经网络对分数函数的逼近与逆向扩散模型生成的概率分布的逼近联系起来。

## Abstract
Score-based diffusion models have achieved remarkable empirical success in generative modeling, yet their approximation-theoretic foundations remain incomplete. In particular, although classical universal approximation theorems guarantee that neural networks can approximate score functions, it remains unclear whether such approximation guarantees translate into approximation of the probability distributions generated by reverse diffusion processes. In this paper, we establish a rigorous quantitative connection between these two notions. Specifically, we prove that if a neural network approximates the true score function sufficiently accurately, then the probability distribution generated by the corresponding reverse diffusion model is close to the target data distribution in Kullback-Leibler (KL) divergence, up to an irreducible mismatch between the terminal distribution of the forward diffusion process and the prior used to initialize the reverse process. More precisely, we derive an explicit upper bound on the distribution approximation error in terms of the score approximation error, the diffusion noise schedule, and the terminal prior mismatch. Our analysis combines Hornik's universal approximation theorem, Girsanov's theorem on path space, and the data processing inequality for relative entropy. Complementary to recent work that studies score approximation under finite-sample statistical settings and structural assumptions on the data distribution, our work develops an approximation-theoretic analysis based on classical neural network approximation theory. The resulting theorem provides a simple and explicit guarantee linking neural network approximation of score functions to approximation of the probability distributions generated by reverse diffusion models.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于分数的扩散模型（score-based diffusion models）在实践中取得了巨大成功，但其理论基础存在一个关键缺口——经典万能逼近定理只能保证神经网络可以**在函数空间上逼近真实分数函数**，但无法直接推出**逆向扩散过程生成的概率分布**能够逼近目标数据分布。两者之间的定量关系此前缺乏严格刻画。
- **研究动机**：填补从“分数逼近”到“分布逼近”的理论空白，建立两者之间的显式数学联系，为扩散模型的表达能力提供理论支撑。
- **整体含义**：本文证明了一个简洁而有力的结论——**分数函数逼近得越好，生成分布与目标分布在KL散度意义下就越接近**，误差上界由分数逼近误差、噪声调度和终端先验失配三者共同控制。

## 2. 论文提出的方法论

### 2.1 核心思想

本文通过**三步递推**建立从分数逼近到分布逼近的理论桥梁：

1. **分数逼近 → 路径测度逼近**（第5节）：利用Girsanov定理，推导出真实逆向扩散过程与学习到的逆向扩散过程在路径空间上的KL散度表达式。
2. **路径测度逼近 → 分布逼近**（第6节）：利用相对熵的数据处理不等式，证明路径测度之间的KL散度可以上界化边际分布（即生成数据分布）之间的KL散度。
3. **结合万能逼近定理**（第7节）：利用Hornik定理保证神经网络可在$L^2(\mu)$意义下任意精确地逼近分数函数，从而得到最终的主定理。

### 2.2 关键技术细节

- **前向扩散过程**：$dX_t = f(X_t, t)dt + g(t)dW_t$，目标数据分布为 $\mu$，边际密度为 $p_t$，分数函数定义为 $s(x,t) = \nabla_x \log p_t(x)$。
- **真实逆向SDE**：$dX_t = [f(X_t,t) - g^2(t)s(X_t,t)]dt + g(t)d\bar{W}_t$。
- **学习到的逆向SDE**：$dX_t^\theta = [f(X_t^\theta,t) - g^2(t)s_\theta(X_t^\theta,t)]dt + g(t)d\bar{W}_t$。
- **路径测度KL散度的显式公式**（Proposition 1）：

$$
\mathrm{KL}(P \parallel Q_\theta) = \mathrm{KL}(p_T \parallel p_{\text{prior}}) + \frac{1}{2}\int_0^T g^2(t)\,\mathbb{E}_{p_t}\left[\|s_\theta(X_t,t) - s(X_t,t)\|^2\right]dt
$$

该公式将路径测度差异精确分解为**终端先验失配**与**分数函数$L^2$逼近误差的累积**之和。

- **分布逼近的主定理**（Theorem 1）：对于任意 $\varepsilon > 0$，存在神经网络 $s_\theta$ 使得 $\|s_\theta - s\|_{L^2(\mu)^d} < \varepsilon$，且生成分布 $\mu_\theta$ 满足：

$$
\mathrm{KL}(\mu \parallel \mu_\theta) < \delta + \frac{1}{2}g_{\max}^2 \varepsilon
$$

其中 $\delta = \mathrm{KL}(p_T \parallel p_{\text{prior}})$ 为先验失配，$g_{\max} = \max_{t\in[0,T]} |g(t)|$ 为扩散系数的最大值。

### 2.3 核心假设

- **正则性假设**（Assumption 1）：漂移 $f$、真实分数 $s$ 和学习到的分数 $s_\theta$ 在空间变量上全局Lipschitz连续，且对时间 $t$ 一致。
- **Novikov条件**（Assumption 2）：保证Girsanov定理可应用，即指数局部鞅是真正的鞅。
- 论文指出这些假设在**方差保持（VP）扩散模型**（数据分布为亚高斯且分数网络至多线性增长）和**方差 exploding（VE）扩散模型**（数据分布具有有限指数矩）等常见设定下均成立。

## 3. 实验设计

- **本文不包含任何实验**。这是一篇**纯理论（approximation-theoretic）论文**，全文聚焦于数学证明和理论推导，未涉及任何数据集、benchmark或实验对比。
- 论文在“相关工作”部分讨论了Chen等人【3】的实证/统计学习工作作为对比，但本文自身并未进行实验验证。

## 4. 资源与算力

- **论文未提及任何计算资源**（GPU型号、数量、训练时长等）。作为理论论文，本文不涉及模型训练或数值实验，因此没有算力相关的讨论。

## 5. 实验数量与充分性

- **本文没有任何实验**，因此不存在实验数量、数据集、消融实验或benchmark对比的问题。
- 作为理论论文，其充分性体现在**数学证明的严谨性和完整性**上——论文从假设出发，通过引理、命题到定理的递进式证明，建立了完整的理论框架。

## 6. 论文的主要结论与发现

- **核心发现**：分数函数的$L^2$逼近误差可以直接且定量地转化为生成分布的KL散度逼近误差。
- **主要定理**（Theorem 1）：对于任意 $\varepsilon > 0$，存在神经网络使得 $\mathrm{KL}(\mu \parallel \mu_\theta) < \delta + \frac{1}{2}g_{\max}^2\varepsilon$。
- **理想情形**：若前向过程的终端分布与逆向初始化的先验分布完全一致（即 $p_T = p_{\text{prior}}$，$\delta = 0$），则分布逼近误差完全由分数逼近误差控制。
- **理论意义**：万能逼近定理对分数函数的保证，可以直接“传递”到对生成分布的保证——**神经网络的万能逼近能力直接转化为扩散模型的万能逼近能力**。
- **与已有工作的关系**：本文与Chen等人【3】的工作互补——后者关注有限样本下分数估计误差向生成分布的传播（统计学习视角），本文关注神经网络在函数空间上的逼近能力向分布空间的传递（逼近论视角）。

## 7. 优点

- **理论简洁性**：最终定理形式简洁、直观，给出了分数逼近误差到分布逼近误差的**显式线性上界**，易于理解和应用。
- **方法论融合**：巧妙结合了三个不同领域的核心工具——**Hornik万能逼近定理**（神经网络逼近论）、**Girsanov定理**（随机分析）和**数据处理不等式**（信息论），展现了跨学科的理论深度。
- **填补理论空白**：首次在通用欧几里得设定下建立了从分数逼近到分布逼近的严格定量联系，无需对数据分布施加低维子空间等结构假设。
- **广泛的适用性**：论文在假设讨论中明确指出，其框架适用于VP扩散、VE扩散等多种常见设定，且对满足线性增长条件的神经网络架构（包括ReLU、GELU、SiLU、Softplus等激活函数的前馈网络、卷积网络、U-Net、Transformer等）均适用。

## 8. 不足与局限

- **缺乏实验验证**：作为纯理论论文，未提供任何数值实验来验证或展示理论界在实际中的紧致性或可达到性。读者无法判断该上界在实践中是否宽松或具有指导意义。
- **正则性假设较强**：全局Lipschitz连续性假设对于任意真实数据分布并不普遍成立（论文自己也承认这一点），限制了定理的适用范围。虽然论文列举了高斯分布和强对数凹分布等满足该假设的例子，但对于更复杂的高维真实数据（如图像），该假设可能不成立。
- **仅考虑KL散度**：分析局限于KL散度，虽然Girsanov定理天然适合这一设定，但KL散度对分布支撑集的变化敏感且不对称，不一定是最适合生成建模的度量。论文在“扩展与未来方向”中提到了Wasserstein距离、全变差距离等其他概率度量的可能性，但并未在本工作中解决。
- **未涉及有限样本分析**：本文关注的是**逼近能力**（是否存在一个足够好的神经网络），而非**估计问题**（能否从有限样本中学习到这样的网络）。实际应用中，样本复杂度、泛化误差等问题同样关键，论文仅将其列为未来方向。
- **未处理离散时间DDPM**：分析基于连续时间SDE框架，未直接推广到离散时间的DDPM。论文在“扩展与未来方向”中提及这一点。
- **限于欧几里得数据**：框架假设数据位于 $\mathbb{R}^d$，未覆盖流形、图等结构化状态空间上的扩散模型。

（完）
