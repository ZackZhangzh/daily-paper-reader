---
title: "GeoCache: Training-Free Acceleration of Multi-View Texture Diffusion via Geometric Delta Transport"
title_zh: GeoCache：基于几何增量传输的无训练加速多视图纹理扩散
authors: "Haotang Li, Zhenyu Qi, Shaohan Henry Wang, Kebin Peng, Yutong Zhao, Zi Wang, Bo Liu, Huanrui Yang, Sen He"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13255v1"
tags: ["query:fm"]
score: 8.0
evidence: 多视图纹理扩散加速
tldr: 几何条件多视图扩散模型可生成高质量3D纹理，但逐视图反复去噪计算成本高昂。现有加速器主要利用时间步冗余，但在纹理生成中跳过步骤会破坏跨视图一致性。本文发现几何对应表面点的预测干净信号具有可迁移演化规律，据此提出GeoCache，仅计算旋转锚点视图并传输其几何对齐的更新至其余视图，周期性全视图计算控制误差。该方法无需重训练，在Hunyuan3D等模型上实现2.21倍加速，同时保持最优保真度，证明跨视图几何是有效的加速维度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13255v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13255v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1850, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13255v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1807, \"height\": 705, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13255v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1826, \"height\": 671, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13255v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1914, \"height\": 1085, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13255v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 469, \"label\": \"Table\"}]"
motivation: 现有训练无关加速器依赖时间冗余，不适合多视图纹理生成，因为跳过步骤会削弱跨视图交互，导致一致性和保真度快速退化。
method: 提出GeoCache插件，选取旋转锚点视图计算每步去噪更新，将几何对齐的预测干净信号传输到其他视图，并周期性执行全视图计算以控制累积误差。
result: 在Hunyuan3D-2.1上获得2.21倍去噪加速，MV-LPIPS 0.0293，MV-PSNR 33.60dB；在SyncMVD和MVPainter上均实现最高加速和最低FLOPs，且保真度最优。
conclusion: 跨视图几何信息可作为多视图纹理扩散的有效加速轴，GeoCache在速度与保真度权衡上优于时间缓存和步数缩减方法。
---

## 摘要
几何条件约束的多视图扩散能够生成高质量的三维纹理，但其逐视图重复的降噪评估带来了显著的计算开销。现有无训练加速器主要利用时间冗余，通过跨降噪步骤复用计算。然而，在多视图纹理生成中，跳过步骤也会移除持续对齐同一表面不同观测的跨视图交互，导致一致性和保真度迅速退化。我们的分析发现了一种互补的冗余来源：尽管中间特征仍具有视图特异性，但几何上对应的表面点在其预测的干净信号中展现出可迁移的演化规律。基于这一观察，我们提出GeoCache，一种无训练插件，它评估旋转的锚视图子集，并将其几何对齐的逐步$x_z$更新传输到其余视图。周期性全视图计算控制累积误差，而采样器一致的重建保持降噪轨迹。GeoCache既不需要重新训练也不需修改架构，并使用几何条件纹理管线中已有的位置图。在Hunyuan3D-2.1、SyncMVD和MVPainter上，GeoCache在大于2倍加速的操作点上相比时间缓存和减少步数方法实现了更强的速度-保真度权衡。在Hunyuan3D-2.1上，它实现了2.21倍的降噪器循环加速，MV-LPIPS为0.0293，MV-PSNR为33.60 dB，在大于2倍加速的所有测试方法中提供了最佳保真度。相同的传输配置在SyncMVD上达到最高加速比和最低FLOPs，而在MVPainter上，GeoCache在加速方法中实现了最低FLOPs和最佳保真度。这些结果确立了跨视图几何作为多视图纹理扩散的有效加速维度。

## Abstract
Geometry-conditioned multi-view diffusion enables high-quality 3D texture generation, but its repeated per-view denoiser evaluations introduce substantial computational cost. Existing training-free accelerators primarily exploit temporal redundancy by reusing computation across denoising steps. In multi-view texturing, however, skipping a step also removes the cross-view interaction that continually aligns different observations of the same surface, leading to rapidly degraded consistency and fidelity. Our analysis identifies a complementary source of redundancy: although intermediate features remain view-specific, geometrically corresponding surface points exhibit transferable evolution in their predicted clean signals. Based on this observation, we introduce \gc{}, a training-free plugin that evaluates a rotating subset of anchor views and transports their geometry-aligned per-step $\xz$ updates to the remaining views. Periodic full-view computation controls accumulated error, while sampler-consistent reconstruction preserves the denoising trajectory. \gc{} requires neither retraining nor architectural modification and uses the position maps already available in geometry-conditioned texturing pipelines. Across Hunyuan3D-2.1, SyncMVD, and MVPainter, \gc{} achieves a stronger speed--fidelity trade-off than temporal caches and step reduction at operating points above $2\times$. On Hunyuan3D-2.1, it delivers a $2.21\times$ denoiser-loop speedup with an MV-LPIPS of 0.0293 and an MV-PSNR of 33.60 dB, providing the best fidelity among all tested methods above $2\times$. The same transferred configuration reaches the highest speedup and lowest FLOPs on SyncMVD, while \gc{} achieves the lowest FLOPs and best fidelity among the accelerated methods on MVPainter. These results establish cross-view geometry as an effective acceleration axis for multi-view texture diffusion.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

**研究动机与背景：**

- 文本/图像驱动的3D资产生成已收敛为两阶段流程：形状模型生成几何，几何条件多视图扩散模型为其着色，最终烘焙为UV纹理【1†L70-L73】。
- **核心瓶颈**：纹理绘制（paint）阶段占据端到端生产成本的67.0%，而其中的去噪循环是神经缓存唯一能干预的环节【1†L73-L74】。
- 现有训练无关加速器（如TeaCache、MagCache、FORA、TaylorSeer等）主要利用**时间轴冗余**——跨去噪步骤复用计算【1†L80-L86】。但在多视图纹理生成中，**跳过步骤会同时移除跨视图交互**，导致同一表面不同观测的一致性迅速崩溃【1†L29-L32】。
- 本文的核心洞察：存在一种**互补的冗余来源**——几何上对应的表面点在其预测的干净信号（$x_0$）中展现出可迁移的演化规律，尽管中间特征仍具有视图特异性【1†L32-L34】。

**整体含义：**
本文首次识别并验证了**跨视图几何冗余**作为多视图纹理扩散的有效加速维度，并据此提出GeoCache——一种无需训练、无需架构修改的即插即用加速插件【1†L99-L101】。

## 二、方法论

**核心思想：**

GeoCache在每一步仅对**旋转的锚点视图子集**（anchor views）运行去噪器，将其几何对齐的$x_0$增量传输到其余视图，而非直接复制锚点的完整状态【1†L159-L162】【1†L37-L39】。

**关键技术细节：**

1. **几何对应算子 $G_{u \rightarrow v}$** ：基于位置图（position maps）预计算，为每个目标视图v的token在源视图u中寻找K个最近邻3D点（容差为包围盒对角线的1%），进行面积加权求和【1†L160-L164】。该算子为固定的行随机矩阵，逐通道作用于token网格，遮挡区域对应行置零【1†L164-L167】。

2. **批量切片锚点前向传播（Batch-sliced anchor forward）** ：在缓存步骤中，仅将N个视图中的a个锚点视图送入去噪器，保留每个视图的结构索引（行列分组、RoPE帧槽），使其位置与原始前向完全一致【1†L167-L172】。每⌈N/a⌉步进行一次全视图刷新，恢复完整的N视图注意力上下文【1†L172-L175】。

3. **增量传输（Delta transport）** ：核心公式为
   $$x_0^{(v)}(t) = x_0^{(v)}(t-1) + \mathcal{G}_{A \rightarrow v}\left[\Delta x_0^{(A)}(t)\right]$$
   其中$\Delta x_0^{(A)}(t) = x_0^{(A)}(t) - x_0^{(A)}(t-1)$为锚点的$x_0$空间一阶差分【1†L177-L185】。非锚点视图保留自己的内容和噪声路径，仅继承共享表面的去噪演化【1†L185-L187】。

4. **采样器一致重建**：将传输后的$x_0$通过标准闭式关系转换回采样器所需的参数化形式（$\epsilon$预测、$v$预测或流匹配速度），保持多步历史的一致性【1†L189-L195】。

5. **漂移界定的调度策略**：使用4次全视图计算来界定额外误差——两步头部建立内容、一次中途刷新重新校准、一步尾部解码【1†L195-L199】。

**算法流程简述：**

- 预计算所有视图对之间的几何对应算子（一次性，基于位置图）。
- 在每个去噪步骤中，选择旋转的锚点子集，仅对其运行去噪器。
- 计算锚点$x_0$的步间差分，通过几何算子传输到所有非锚点视图。
- 非锚点视图将自己的上一状态与传输来的增量相加。
- 周期性执行全视图刷新以控制累积误差。
- 将更新后的$x_0$转换回采样器原生参数化形式，继续下一【1†L173-L175】【1†L187-L189】步。

## 三、实验设计

**数据集与基准：**

- 主基准：**eval200**——固定列表的200个资产（100个GSO + 100个Objaverse）【1†L242-L243】。
- 扩展基准：**TexVerse-100**和**ABO-100**（附录中报告）【1†L243-L244】。

**评估模型（三个主干）：**

- **Hunyuan3D-2.1 Paint**：6视图，15步UniPC采样器【1†L125-L126】【1†L240】。
- **SyncMVD**：10视图，30步DDPM轨迹【1†L258-L259】。
- **MVPainter**：75步去噪轨迹【1†L277】。
- 附录中还测试了**MV-Adapter**（50步Euler采样器）【1†L337-L338】。

**对比方法：**
覆盖步缓存分类学的各个维度——TeaCache、MagCache、FORA、TaylorSeer、FasterCache-CFG、DeepCache，以及**步数缩减**（step reduction）【1†L244-L247】。所有方法在同一资产、同一随机种子上进行评分【1†L247-L249】。

**评估指标：**
- **MV-LPIPS**和**MV-PSNR**：加速运行与原始运行在同一视图上的感知和像素级差异，取N个视图平均【1†L252-L253】。
- **FLOPs**：每个资产的去噪器总浮点运算量【1†L253】。
- **Time**：实测墙钟时间【1†L253】。
- 补充指标（附录）：MV-LPIPS的p95尾值、SeamErr、烘焙纹理分数（TexPSNR、UV-PSNR、UV-LPIPS）【1†L256-L257】。

## 四、资源与算力

**论文未明确说明训练所用的GPU数量或训练时长**，因为GeoCache是**训练无关**（training-free）方法，无需模型重新训练【1†L38-L39】。

**推理硬件配置：**
所有实验在**单张NVIDIA RTX 4090**上运行，峰值显存23.3 GB【1†L240-L242】。去噪器循环速度提升为**循环级**（loop-level）加速比，而非端到端加速比【1†L76-L78】。在默认6×512²配置下，端到端加速为1.07倍，绘制阶段加速为1.11倍【1†L327-L329】。

## 五、实验数量与充分性

**实验组数：**

1. **三个主干模型**的完整评估（Hunyuan3D-2.1、SyncMVD、MVPainter），每个主干与各自适配的基线对比【1†L239-L249】。
2. **两个额外主干**（MV-Adapter）的扩展测试【1†L337-L338】。
3. **四个资产池**：eval200（主）+ TexVerse-100 + ABO-100【1†L242-L244】。
4. **消融实验**（表2）：测试了增量传输vs直接复制、刷新位置vs刷新次数、锚点数量、对应关系打乱等关键设计决策【1†L304-L321】。
5. **附录中完整的方法论分类对比**（全量对比在Hunyuan上）【1†L247】。

**充分性与客观性评估：**

- **充分**：覆盖了三种架构差异显著的主干（UniPC、DDPM、长轨迹），对比了步缓存分类学中的所有主要方法类别，并进行了消融实验验证关键设计。
- **客观与公平**：所有方法在同一资产、同一随机种子上评估【1†L248-L249】；GeoCache的Hunyuan调优配置直接迁移到SyncMVD且仍领先【1†L263-L265】；每个 fidelity 值报告均值和标准差【1†L255】。但也存在局限（见第八部分）。

## 六、主要结论与发现

1. **时间缓存不适用于多视图纹理**：在约2.1倍加速下，MagCache和TeaCache引入显著的跨视图不一致（SeamErr升至1.21倍原始值）【1†L136-L138】；步数缩减虽保持接缝误差低于原始，但激进缩减会丢失局部纹理细节【1†L140-L141】。

2. **跨视图几何是有效的加速维度**：中间特征在几何对应token上的余弦相似度仅0.362，仅比随机配对高0.10，说明特征具有视图特异性【1†L148-L150】；但$x_0$的**增量**是可迁移的——传输增量而非完整状态将误差从0.090降至0.025 MV-LPIPS【1†L155-L157】。

3. **GeoCache在速度-保真度权衡上全面领先**：
   - **Hunyuan3D-2.1**：2.21倍加速，MV-LPIPS 0.0293，MV-PSNR 33.60 dB——在大于2倍加速的方法中保真度最佳【1†L263-L265】。每增加0.1倍加速仅损失3.1% MV-LPIPS，而时间缓存损失12.4%~33.5%【1†L268-L270】。
   - **SyncMVD**：2.60倍加速（最快）、16.24 TFLOPs（最低），比15步缩减精度高11%，比TaylorSeer高39%【1†L275-L277】。
   - **MVPainter**：4.04倍加速、MV-LPIPS 0.0282——同时最快、最省、最准【1†L279-L281】。

4. **增量传输是最关键的设计决策**：直接复制锚点$x_0$值的LPIPS误差是增量传输的2.8倍【1†L309-L310】。

## 七、优点（亮点）

1. **首次识别新加速维度**：首次系统性地识别并验证了跨视图几何冗余作为多视图纹理扩散的加速轴，超越了传统的时间轴复用范式【1†L99-L101】。

2. **训练无关且即插即用**：无需重新训练或架构修改，仅需使用几何条件纹理管线中**已有的位置图**【1†L38-L39】【1†L37-L39】。

3. **设计优雅且理论清晰**：增量传输机制保留了每个视图自身的内容和噪声路径，仅继承共享表面的去噪演化，避免了直接复制带来的方向噪声污染【1†L177-L187】。

4. **跨主干泛化性强**：在Hunyuan3D-2.1上调优的配置直接迁移到SyncMVD仍领先所有指标【1†L263-L265】；在MVPainter上同时达到最高加速、最低FLOPs和最佳保真度【1†L279-L281】。

5. **速度-保真度曲线平坦**：每0.1倍加速的保真度损失仅为时间缓存的1/4到1/11，说明该方法在更激进的加速设置下仍保持稳健【1†L268-L270】。

6. **广泛的实验验证**：覆盖三个主干、四个资产池、七种基线方法，并进行了系统的消融实验【1†L239-L249】【1†L304-L321】。

## 八、不足与局限

1. **依赖几何对应的质量**：有效性取决于位置图或等价表示的可见性和准确性。被少数锚点观察的表面区域接收的传输更新较少，更依赖于周期性全视图计算【1†L333-L336】。

2. **从去噪器节省到系统级延迟的转化受限**：端到端加速取决于绘制阶段中去噪所占据的时间比例——在默认6×512²配置下仅1.07倍端到端加速【1†L327-L329】。硬件利用率也影响转化（SyncMVD上4.1倍FLOPs缩减仅产生2.60倍墙钟加速，因内核启动成为瓶颈）【1†L330-L332】。

3. **长去噪轨迹下优势减弱**：在MV-Adapter的50步Euler采样器上，TaylorSeer达到更高保真度（MV-LPIPS 0.0021 vs GeoCache的0.0052），尽管GeoCache仍保持最低接缝误差【1†L337-L339】。

4. **保真度测量范围的局限**：保真度是在固定随机种子下相对于原始模型的测量值，仅隔离了加速引入的近似误差，未涵盖种子鲁棒性以及不同材质和光照下的渲染空间度量【1†L339-L341】。

5. **未提及训练资源**：作为训练无关方法虽无需训练，但未提供任何关于方法开发过程中的计算资源信息。

（完）
